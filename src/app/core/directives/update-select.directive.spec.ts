import { UpdateSelectDirective } from './update-select.directive';

describe('UpdateSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateSelectDirective();
    expect(directive).toBeTruthy();
  });
});
